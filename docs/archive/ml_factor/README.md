# Archived `ml_factor` Module

这里保存的是已经下线或不再接入当前 Flask 主应用的历史文档。

## Why archived

- 当前运行中的页面和 API 已不再注册 `ml_factor` 模块
- 原有前端页面资源已经从主代码路径移除
- 这些文档保留历史背景，但不应再作为当前实现依据

## How to use

如果未来要重建该模块，可以把这些资料当作历史参考，但建议重新梳理接口、页面和目录，而不是直接恢复旧文件。
